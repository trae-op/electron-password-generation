import { TwoFactorVerifyWindow, Provider } from "@ui-business/TwoFactor";

const TwoFactorVerify = () => {
  return (
    <Provider>
      <TwoFactorVerifyWindow />
    </Provider>
  );
};

export default TwoFactorVerify;
